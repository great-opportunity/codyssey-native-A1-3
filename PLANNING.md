# 서비스 기획서 — Rewind

## 1. 서비스 목적

화상영어 수업을 듣고 나면 그 순간엔 고쳐야 할 표현이 기억나지만, 시간이 지나면 흐릿해지고
같은 실수를 반복하게 된다. **Rewind**는 수업 대화 내용을 붙여넣기만 하면 AI가 학습자 본인이
말한 문장 중 어색한 부분을 짚어주고, 왜 어색한지·어떻게 고치면 자연스러운지를 정리해주는
**수업 후 복습 도구**다.

## 2. 타겟 사용자

- 화상영어(또는 오프라인 회화 수업)를 정기적으로 듣는 성인 영어 학습자
- 수업이 끝나면 스스로 뭘 틀렸는지 정리할 시간/전문성이 부족한 사람
- 반복되는 실수 패턴(시제, 관사, 어순 등)을 데이터로 확인하고 싶은 사람

## 3. 페이지/섹션 구성

| 페이지 | 목적 |
| --- | --- |
| 홈 (`index.html`) | 서비스 가치 제안, 3단계 사용법, CTA |
| 분석하기 (`analyze.html`) | 핵심 기능 — 텍스트/txt/mp3 입력 → AI 분석 → 결과 확인 |
| 가이드 (`guide.html`) | 수업 기록 준비 방법, 자주 하는 실수 유형, FAQ |

3개 페이지는 상단 고정 내비게이션(모바일에서는 햄버거 메뉴)으로 자유롭게 이동 가능하다.

## 4. 핵심 기능 (AI 기능) 정의

**기능명**: 화상영어 발화 분석 및 학습 포인트 제공

- **입력**
  - 방식 A: 대화 텍스트 직접 붙여넣기 (`Me:` / `Tutor:` 화자 표시 권장, 없어도 동작)
  - 방식 B: txt 파일 업로드
  - 방식 C: mp3 파일 업로드 (3MB 이하, 약 2~3분) → Whisper API로 자동 전사 후 동일 파이프라인 진행
- **처리**
  1. (mp3인 경우) `POST /api` (`action: "transcribe"`) → OpenAI Whisper API로 텍스트 전사
  2. `POST /api` (`action: "analyze"`) → OpenAI GPT(`gpt-4o-mini`)가 화자 표시를 참고해 학습자 발화만 추출,
     어색한 문장을 찾아 원문/자연스러운 표현/이유(문법·표현 포인트, 한국어 설명)를 JSON으로 반환
- **출력**
  - 전체 총평 한 줄 요약
  - 어색한 문장별 카드: 원문 → 교정 표현 → 이유
  - 어색한 문장이 없으면 긍정적인 피드백 메시지
- **사용자에게 제공하는 가치**
  - 수업 직후 몇 초 안에 "오늘 내가 뭘 고쳐야 하는지" 명확히 파악
  - 단순 교정이 아니라 이유를 알려줘서 같은 실수 재발 방지

## 5. AI 기능 실패 처리 기준

| 상황 | 처리 |
| --- | --- |
| 빈 입력(텍스트 없음/파일 미선택/내용 없음) | 요청 전 프론트에서 검증, "분석할 텍스트를 입력해주세요" 등 안내 후 요청 차단 |
| 오디오 파일 3MB 초과 | 업로드 전 크기 체크 후 "txt로 업로드해주세요" 안내, API 호출하지 않음 |
| API 오류 (4xx/5xx) | 서버가 상태 코드와 함께 사용자 친화적 에러 메시지 반환 → 화면 상단 에러 박스에 표시 |
| 응답 지연/타임아웃 | 프론트에서 30초 타임아웃(AbortController) 설정, 초과 시 "응답이 지연되고 있어요" 안내 후 재시도 유도 |

## 6. 비고 / 제약

- 서버에 사용자의 대화 내용을 별도 저장하지 않는다(요청-응답 처리 후 폐기).
- mp3 전사는 Vercel 서버리스 함수의 요청 본문 크기 제한 때문에 짧은 파일(3MB 이하)만 지원하며,
  긴 녹음은 다른 도구로 텍스트 변환 후 txt 업로드를 안내한다.
