# 서비스 기획서 — Rewind

## 1. 서비스 목적

화상영어 수업을 듣고 나면 그 순간엔 고쳐야 할 표현이 기억나지만, 시간이 지나면 흐릿해지고
같은 실수를 반복하게 된다. **Rewind**는 수업 대화 내용을 붙여넣기만 하면 AI가 학습자 본인이
말한 문장 중 어색한 부분을 짚어주고, 왜 어색한지·어떻게 고치면 자연스러운지를 정리해주는
**수업 후 복습 도구**다.

## 2. 타겟 사용자

- 화상영어(또는 오프라인 회화 수업)를 정기적으로 듣는 성인 영어 학습자
- 수업이 끝나면 스스로 뭘 틀렸는지 정리할 시간/전문성이 부족한 사람
- 반복되는 실수 패턴(시제, 관사, 어순 등)을 데이터로 확인하고 싶은 사람

## 3. 페이지/섹션 구성

| 페이지 | 목적 |
| --- | --- |
| 홈 (`index.html`) | 서비스 가치 제안, 3단계 사용법, CTA |
| 분석하기 (`analyze.html`) | 핵심 기능 — 텍스트/txt/mp3 입력 → AI 분석 → 결과 확인 |
| 가이드 (`guide.html`) | 수업 기록 준비 방법, 자주 하는 실수 유형, FAQ |

3개 페이지는 상단 고정 내비게이션(모바일에서는 햄버거 메뉴)으로 자유롭게 이동 가능하다.

## 4. 핵심 기능 (AI 기능) 정의

**기능명**: 화상영어 발화 분석 및 학습 포인트 제공

- **입력**
  - 방식 A: 대화 텍스트 직접 붙여넣기 (`Me:` / `Tutor:` 화자 표시 권장, 없어도 동작)
  - 방식 B: txt 파일 업로드
  - 방식 C: 녹음 파일 업로드 (m4a·mp3·wav, 20분 내외)
- **처리**
  1. (녹음인 경우) 브라우저에서 16kHz 모노 mp3(48kbps)로 압축하고 2분 30초 단위로 분할 →
     `POST /api?action=transcribe&offset=<초>`를 동시에 호출 →
     `gpt-4o-transcribe-diarize`가 화자를 나눠 받아쓰고, 결과를
     `[분:초] 화자: 발화` 형태로 합침
  2. `POST /api` (`action: "analyze"`) → `gpt-5-mini`가 화자 표시(없으면 문맥)로
     학습자 발화만 추출, 어색한 문장을 찾아 원문/교정문/이유(한국어)를 JSON으로 반환
- **출력**
  - 전체 총평 한 줄 요약
  - 반복되는 실수 패턴: 유형 이름 + 횟수 + 다음 수업에 의식할 점
  - 어색한 문장별 카드: (녹음 시각) → 원문 → 교정 표현 → 이유
  - 어색한 문장이 없으면 긍정적인 피드백 메시지
- **사용자에게 제공하는 가치**
  - 수업이 끝나면 녹음 파일 하나만 올려도 복습 자료가 완성됨
  - 단순 교정이 아니라 이유를 알려줘서 같은 실수 재발 방지
  - 반복 패턴으로 자기 약점을 파악하고, 표시된 시각으로 실제 발화를 다시 확인 가능

## 5. AI 기능 실패 처리 기준

| 상황 | 처리 |
| --- | --- |
| 빈 입력(텍스트 없음/파일 미선택/내용 없음) | 요청 전 프론트에서 검증, "분석할 텍스트를 입력해주세요" 등 안내 후 요청 차단 |
| 압축해도 조각이 용량 상한을 넘음 | 업로드 전에 감지해 "더 짧게 잘라서 올려주세요" 안내, API 호출하지 않음 |
| 오디오 디코딩 실패(손상·미지원 형식) | "녹음 파일을 변환하지 못했어요" 안내 후 중단 |
| AI 호출 실패 | 예외 종류로 원인을 구분해 서로 다른 상태 코드와 안내를 보냄(지연 504 / 한도·잔액 429 / 설정 오류 500 / OpenAI 장애 502). 사용자에게는 본인이 할 수 있는 일만 알리고, 자세한 원인은 서버 로그에 기록 |
| 전사 조각 일부 실패 | 실패한 구간만 빼고 나머지로 분석을 진행하고, 몇 구간이 빠졌는지 안내. 전부 실패한 경우에만 중단 |
| 응답 지연/타임아웃 | 프론트에서 타임아웃(AbortController) 설정, 초과 시 "응답이 지연되고 있어요" 안내 후 재시도 유도. 진행 중에는 경과 시간을 표시 |

전체 목록과 각 상황의 실제 메시지는 [`submission/error-handling.md`](submission/error-handling.md)에 정리했다.

## 6. 비고 / 제약

- 서버에 사용자의 대화 내용이나 녹음을 별도 저장하지 않는다(요청-응답 처리 후 폐기).
- 20분 녹음 기준 전체 처리에 4~5분이 걸린다(압축 30초 + 전사 2분 + 분석 1~2분).
  진행 중 창을 닫으면 처음부터 다시 해야 한다.
- 녹음을 조각내어 전사하므로 화자 기호(A/B)가 조각마다 다른 사람을 가리킬 수 있고,
  같은 사람이 여러 기호로 갈리기도 한다. 두 가지로 보완했다. 첫째, 음질을 너무 낮추지
  않아 음색 정보를 남긴다. 둘째, 분석 단계에서 각 기호가 학습자인지 튜터인지 판정하게
  하고, 그 결과로 전체 대화를 "나 / 튜터"로 다시 라벨링해 보여준다.
- 받아쓴 전체 대화를 결과 아래에 함께 보여준다. 녹음을 올린 경우 무엇이 입력됐는지
  확인할 다른 방법이 없기 때문이다.
- 20분을 크게 넘는 녹음은 나눠서 올려야 한다.
